{
  "name": "@simplytokenized/interview-kit",
  "version": "1.0.0",
  "description": "The rules that decide what a generated contract contains — condition evaluation, conditional passages, repeating blocks and document assembly. One implementation shared by the network manager's authoring UI and the backend's assembler.",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "files": [
    "src"
  ],
  "main": "./src/index.js",
  "types": "./src/index.d.ts",
  "exports": {
    ".": {
      "types": "./src/index.d.ts",
      "default": "./src/index.js"
    },
    "./fixtures/golden.json": "./src/fixtures/golden.json",
    "./package.json": "./package.json"
  },
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
